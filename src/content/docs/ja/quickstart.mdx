---
title: クイックスタート
description: SnapVM コンソールにサインインし、マシンを作成してターミナルを開きます。
---

このガイドでは、Web コンソールから最初の SnapVM ワークスペースを作成します。ローカルターミナルで作業したい場合は、マシンの作成または選択後に [SnapVM CLI](/ja/cli/installation) をインストールします。

## 前提条件

- SnapVM のアカウント、招待、またはプライベートベータアクセス
- モダンブラウザ
- マシンを作成できる組織

## 最初のマシンを作成する

1. SnapVM コンソールを開いてサインインします。
2. 組織を選択します。案内が出た場合は組織を作成します。
3. **Machines** に移動し、**New** を選択します。
4. 小文字英数字とハイフンでマシン名を入力します。
5. デプロイで提供されているランタイムやテンプレートを選択します。
6. フォームを確定し、マシンが到達可能になるまで待ちます。

マシンを作成すると、SnapVM がワークスペースを準備し、コンソールからターミナルまたはアプリケーション URL を開ける状態にします。

## ターミナルを開く

マシンの行または詳細ページから **Terminal** を開きます。コンソールはマシン内の対話シェルを開きます。

いくつかコマンドを実行してみます。

```text
pwd
uname -a
mkdir -p ~/projects/hello
cd ~/projects/hello
printf 'hello from snapvm\n' > README.md
```

ブラウザタブを閉じた場合は、同じマシンを開き直してターミナルへ再接続します。直近のセッション出力を確認でき、ファイルシステム上の変更はマシンに残ります。

## HTTP アプリを公開する

多くのデプロイでは、マシンの HTTPS URL がマシン内の HTTP ポートへルーティングされます。デフォルトのアプリケーションポートを使う場合、次のようにサーバーを起動できます。

```text
cd ~/projects/hello
python3 -m http.server 8080
```

その後、コンソールからマシン URL を開きます。URL は通常、次の形式です。

```text
https://<machine-name>.snapvm.app
```

ドメイン、デフォルトポート、認証方式はデプロイごとに異なる場合があります。

## 任意: CLI を使う

CLI をインストールしたら、サインインして作成したマシンを対象にします。

```text
snapvm login
snapvm use <machine-name>
snapvm exec pwd
snapvm console
```

ブラウザ中心の作業にはコンソールを使い、ローカルターミナルからコマンドを実行したい場合は CLI を使います。

## 停止またはハイバネーション

作業が終わったら、停止操作が提供されている場合はコンソールからマシンを停止します。永続ファイル、パッケージ、リポジトリは次回の復帰時にも残ります。長時間動かすプロセスは、ターミナルに置いたままではなくサービスとして定義してください。

## 次に読むページ

- [SnapVM の使い方](/ja/working-with-snapvm)
- [CLI のインストール](/ja/cli/installation)
- [マシン](/ja/concepts/machines)
- [ターミナルセッション](/ja/concepts/terminal-sessions)
